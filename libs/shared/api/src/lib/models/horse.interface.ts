import { Character } from "./character.interface";

export interface Horse {
    id: string;
    // character: Character;
    characterId: string;
    name: string;
    breed: string;
    age: number;
  }
  