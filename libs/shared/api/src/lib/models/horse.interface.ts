import { Character } from "./character.interface";

export interface Horse {
    _id: string;
    // character: Character;
    characterId: string;
    name: string;
    breed: string;
    age: number;
  }
  